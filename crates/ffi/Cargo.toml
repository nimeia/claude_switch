[package]
name = "claude-switch-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "C ABI for claude-switch-core (Windows P/Invoke, etc.)"
publish = false

[lib]
name = "claude_switch"
crate-type = ["cdylib", "staticlib", "rlib"]

[lints]
workspace = true

[dependencies]
claude-switch-core = { path = "../core" }
serde_json = "1"
parking_lot = "0.12"

[dev-dependencies]
tempfile = "3"
serde_json = "1"
