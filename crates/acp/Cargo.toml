[package]
name = "claude-switch-acp"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "ACP (Agent Client Protocol) client for driving Claude Code as a managed agent"
publish = false

[lints]
workspace = true

[dependencies]
claude-switch-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "acp-run"
path = "src/bin/acp_run.rs"
